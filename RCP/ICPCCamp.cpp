#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
    int n,m,s,k;
    cin>>n>>m>>s>>k;
    int A[m];
    int B[s];
    for(int x=0; x<m; x++)
    {
        cin>>A[x];
    }for(int y=0; y<s; y++)
    {
        cin>>B[y];
    }
    sort(A,A+m);
    sort(B,B+s);
    for(int x=0; x<m;x++)
    {
        if()
    }
}
